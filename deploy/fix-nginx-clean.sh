#!/bin/bash
# DEPRECATED: This script is deprecated in favor of install-nginx-config.sh
# which uses static configuration files from the repository.

echo "⚠️  This script is deprecated. Please use install-nginx-config.sh instead."
echo "    install-nginx-config.sh uses static configuration files from the repository"
echo "    for better version control and consistency."
echo ""
echo "Usage: sudo ./deploy/install-nginx-config.sh [minimal]"
exit 1